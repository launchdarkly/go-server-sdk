LaunchDarkly Server-side AI SDK for Go
==============================================

This package has moved to its own repository:
**[launchdarkly/go-server-sdk-ai](https://github.com/launchdarkly/go-server-sdk-ai)**

Update your import path:

```go
import "github.com/launchdarkly/go-server-sdk-ai/ldai"
```

Version numbers continue from where this package stopped. The last release from
this repository was `0.9.4`. The next release is `0.10.0`, from the new
repository.

The [CHANGELOG](./CHANGELOG.md) in this directory keeps the release history up
to `0.9.4`.
